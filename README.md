##Overview##

This Java application validates whether given strings are real English words (using an external dictionary API).  
It also provides additional functionality:
If a single string is given it checks if it is a palindrome.
If two strings are given it checks if they are anagrams of each other.

The project is built with Maven, tested using TestNG and Cucumber (BDD).
